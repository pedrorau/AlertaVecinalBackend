ktor {
    deployment {
        port = 8080
    }
    application {
        modules = [ com.alertavecinal.ApplicationKt.module ]
    }
}

jwt {
    secret = "test-secret-key-minimum-256-bits-required-for-hs256"
    issuer = "https://test.supabase.co/auth/v1"
    audience = "authenticated"
}

database {
    url = "jdbc:postgresql://localhost:5432/alertavecinal_test"
    user = "postgres"
    password = "postgres"
    driver = "org.postgresql.Driver"
    poolSize = 5
}
